# PM010A — cited agent continuity

Design recorded before implementation. Base IdleProof a37cb4fe7a9e1a2f51522e7e81529f83de6a107c.
Core contexts already expose the assertion ProjectEvent ID and SHA256; the agent
renderer currently discards them, and both its own budget and the outer hook can
cut a memory row. Validate and retain these references beside the selected claim.
Validate confirmation-to-assertion linkage when the assertion reference is present.
Legacy contexts without a source remain readable and explicitly uncited.

Each row carries its original epistemic label, assertion event/hash and separate
DECLARED confirmation event. Include context event-head/tree anchors. Budget on
complete lines and allocate the continuity budget after the active-task header so
the outer hook cannot cut a citation. References are advisory provenance, not a
local re-verification of Core's journal and never new Proof authority.

The Portal projection remains its existing allowlisted contract during this lot;
the separate Portal ingestion parity correction must qualify before future citation
projection. This does not close complete PM010 cross-product handoff, storage,
reconciliation or HUMAN acceptance. Full tests and real three-OS producer CI required.

## Local qualification

Two new regression cases fail before implementation. Final249/249 tests pass. Actual installed Core journey preserves assertion/review citations, excludes retired memory and retains 12 native Unicode task cases. A real long-header hook returns5652 characters with four complete cited rows and an explicit truncation notice. Source identities retain their full bounded length. The initial candidate omitted loading for malformed active-task IDs; see the legacy correction below. Same-event review/assertion identities are rejected consistently with the Portal review correction.

Actual optional Python/TypeScript/JSON hooks pass once each at p95 58.2/72.5/75.2ms with unchanged limits. npm pack dry-run passes. Core main8c0481f has qualified24+3 gates. This branch still needs its own fresh review, PR CI and exact-main qualification. Original failures remain in the manifest.

## Cross-product category correction

Initial candidate0db2fd8 passed16/16 CIattempt1 and review5783770122 found no major issues. Portal review4076214883 exposed a shared task-lifecycle admission gap. A regression reproduced it; task contexts now reject confirmations because Core supports lifecycle only on objectives, decisions, invariants and failed approaches. Existing unreviewed task contexts remain readable. Corrected249/249 tests and actual Core/hook journey pass. New exact-source CI/review required.

## Legacy-session review correction

Review4076324524 identified that existing pre-current-format IDs must remain usable. A new public-hook regression failed before correction. Well-formed legacy IDs retain their full identity (up to512 characters), active objective and existing semantic Core lookup. Malformed/oversized IDs display an explicit unavailable-identity marker and suppress only the lookup; persisted IDs are never rewritten. Header length remains bounded before computing the citation allowance. Final250/250 tests and the actual Core journey pass, including legacy header/context/citations. The prior source0b26780 passed16CI jobs but had this finding; fresh final review/CI required.

The first full-suite reporter attempt exited0 but retained only59lines without totals; it is incomplete and does not qualify the source. Its raw log remains archived. An explicit TAP diagnostic verifies returncode0,250tests,250passes,0failures/0skips/0cancellations in17.035s. The manifest points to this complete report rather than inferring success from the first exit code.

## Unicode controls and retained CI latency failure

Head a4260107cdcd64597d8871248ffbc1e2280f16d6 passed15/16CI jobs in run35783376406, attempt1. Linux actual-Core job106934110037 failed the unchanged Python hook p95 limit:222.0ms versus150ms. TypeScript/JSON latency gates were not run. This run remains FAILED; no rerun was requested. Review4076450401 independently found U+0085 NEL escaping the legacy-ID guard. The correction rejects the complete C1 control range, including NEL, in addition to existing whitespace/C0/DEL rejection. New public-hook cases fail before and pass after, preserving the stored ID and active objective. Full250/250 TAP tests pass; the actual Core legacy/citation journey passes.

Diagnostic investigation: the changed loading code only executes for UserPromptSubmit; the timed PreToolUse/PostToolUse path, context extractor, delivery and Core pin match the prior source. One local instrumented sample had p9551.6ms;43Core extraction subprocesses accounted for2.042s, median45.29ms. This does not explain the CI failure and is not a replacement qualification. The CI log lacked the per-hook distribution, so the existing perf gate now prints all80observations, event types, platform and unchanged budgets before its assertions, including on failures. No timing threshold, workload, verification or source freshness boundary changes. The new source requires fresh CI; root cause of the previous CI slowdown remains unresolved. No scheduling cause is asserted and no old failure is relabeled PASS.

## Entity control parity and performance diagnostics

Review4076598615 found that full entity IDs still admitted C1 characters, including suffixes beyond the old96-character display boundary. The entity contract and retained task header now share one bounded identity predicate. All32 C1 characters are refused; safe long identities remain unchanged. The first regression draft used a missing test helper and is retained as invalid evidence; the corrected test reproduces the admission error before and passes after.

Head13d967f8cd8f13ab4790dba660f9b9721f54da9c CI35785063986 fails the default-provider quality hook gate at p95180.9ms (job106939755207), while its actual-Core Python/TS/JSON hooks pass at63.4/89.0/90.9ms (job106939755779). The failed sample contains long PreToolUse calls too, including283.9ms, so the slowdown is not specific to the Core extraction subprocess. No cause is established. Both failed runs remain failures; neither was rerun.

Add a diagnostic-only observer after a failing performance job. It records inclusive synchronous filesystem/subprocess durations and process CPU/resource counters, with no file paths, command arguments, source contents or credentials. The original unchanged gate runs first and remains failed regardless of the observer result. Instrumented timings never qualify latency; no retries or threshold changes are used to turn a failure into a pass.

## Diagnostic provider selection correction

Head9272547fd4199a9b9d01b6078a4e64ead7adf124 passed all16CI jobs in run35786581779, attempt1. This does not explain or erase the two prior latency failures. Review4076701641 found that the Core diagnostic always selected Python even if TypeScript/JSON was the failed gate. Each gate now has its own step ID and a matching observer conditioned on that exact step's failure; default-quality diagnostics likewise require the performance step to have failed. Unexecuted or successful gates do not trigger diagnostic replay. Product source/tests and all budgets remain unchanged. Fresh workflow qualification/review required.
