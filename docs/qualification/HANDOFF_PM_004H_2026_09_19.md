# DiffWitness / IdleProof — PM-004h handoff

Read Core issue#74, CONTRIBUTING, PM_004H.md and the exact closure evidence.
Global NOT ALPHA READY; HUMAN NOT RUN. Preserve all existing work and fetch
remote refs before mutation. No subagents used; all journeys here are MACHINE.

This unit generalizes the existing source-byte consumer to JS/TS/Go/Rust. One
bounded configured argv subprocess, admitted source reads, full schema/path/hash/
provider/module/authority validation and detached cache remain. Python exports
remain wrappers around shared admission. No source execution, network or runtime
dependency install; no required-assurance weakening. Optional unparsed results
stay empty; unavailable/older Core retains explicit legacy fallback coverage.

Base IdleProof30a2c5363d49b86d68574214b47c112c6bb75cee. Core CI pin is
0c342bc2fb12edb2d58da39958e3c0667a22a1db, tree07866b4c51b1670aa26b0267b48eefa4bc45f9c7
(merged#106). Its fresh-main qualification is required before publication/merge.
Actual candidate/main SHAs and gates belong to#74; do not infer remote qualification
from this pre-publication handoff. Existing16 jobs and fresh exact-main replay
remain mandatory; actual optional/no-grammar consumers now run on three OS.

Local222/222 PASS/no skips,15.086s; nine contract tests, actual optional/base
consumer, Explain30/adversarial238/IdleBench5/corpus15 and installed npm package
PASS. With corrected Core source, latest TS p9588.2/max99.4/Stop118.1/state.2ms;
Python83.6/87.8/118.7/.2ms. Existing150/500/750/100ms budgets unchanged.
Small shared-host diagnostics do not guarantee every admitted input's latency.

Retain failures: actual old consumer selected a fake TypeScript function from a
template; three generic-adapter tests failed before implementation. Initial
actual harness expected edits to current TypeScript while the existing selection
rule chose last-touched Rust; explicit read capability corrected the fixture.
No product defect or human evidence is invented from that harness mistake.

Canonical adoption covers task symbols/imports for the listed languages. Routes,
tables, technology signals, feature-model graph and additional languages remain
open. Core PM004i Java/Kotlin/C# is isolated development and must not be claimed
consumed here. Full languages/adapters, bootstrap/longitudinal identity, drift,
cited context and FR/EN retrieval, history/impact, multi-writer, Portal memory/UI/
grounded Q&A, explicit user memory, PM018, real-human tests and release#80 remain
in dependency order. No full PM removal, early tag/release or premature#74 closure.

## Review correction — local/platform imports

Initial PR#11 head d1062268a35e01f34130ccbcca2d021a58432c67 had CI35412716165
16/16 PASS. Review4051821830 nevertheless reproduced local and platform imports
becoming third-party integration signals. Initial green qualification is retained
and is not transferred to the correction. Actual Core reproduction and new smoke
both FAIL before the fix; logs are archived.

Canonical extraction preserves every raw import. At the task dependency boundary,
filter relative/absolute paths, package-internal # names, Node/file/data prefixes,
Node built-ins, Rust crate/self/super/std/core/alloc, and undotted Go module roots.
Node isBuiltin is a runtime metadata query, not project/module execution:
https://nodejs.org/api/module.html#moduleisbuiltinmodulename
Actual TS/Rust/Go checks require empty dependencies/core role for local/platform
only sources, retain candidate external references, and verify raw imports remain.
The regression now runs with real Core on all three OS in the existing16 jobs.

This is conservative textual selection, not semantic package-origin resolution.
JS aliases, host-qualified Go self-modules and Python package origin remain open;
raw observations do not establish third-party ownership, package execution or Proof.
No resolver, shell, runtime dependency install or second extraction system added.

Corrected local222/222 PASS, no skips,15.844s; quality30/238/5/15 and installed
npm package PASS. Actual optional/base consumers PASS. Fresh sequential shared-host
diagnostics after the suites: TS p9585.4/max92.1/Stop132.7/state.4ms;
Python79.9/88.0/117.1/.2ms. Existing budgets and500ms subprocess unchanged.
Corrected-head CI, review and fresh exact-main qualification remain pending.
