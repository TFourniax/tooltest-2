

# PM-004l — adopt remaining canonical task providers

Acceptance before implementation,2026-09-19. Base qualified IdleProof042c94c.
Publication waits for qualified Core PM004k main, including its corrected PM004j
parent. Use local installed Core only for development evidence; no remote PASS
is inferred. Global NOT ALPHA READY; HUMAN NOT RUN.

Extend the existing bounded consumer spec map and task context to Java/Kotlin/C#,
Ruby/PHP, SQL and JSON/TOML/YAML. Same source/path/hash/provider/module/authority
admission, one500ms argv subprocess, detached byte-bound cache and existing limits.
Preserve Python/current-language compatibility APIs. Raw import references of new
families must not be described as proven third-party ownership: keep neutral
references with unresolved origin; do not feed unclassified references into the
third-party dependency signal. No manifest resolver, network or project execution.

For canonical SQL use observed DDL table names. For canonical configuration use
complete escaped key paths, no values. Do not reparse raw config/SQL literals with
legacy route/table/technology heuristics; empty/unparsed remains empty. Preserve
explicit missing/old Core fallback. Other code-language routes/technology and the
full feature-map graph are separate still-open canonical-adoption work.

Before code, reproduce real old-consumer string/value decoys and missing generic
admission. Extend contract tests and actual installed optional/base smoke across
all new families; source change/cache detachment, malformed syntax, import-origin
conservatism and config-value omission required. Existing222 tests, quality/package,
unchanged150/500/750/100ms hook budgets and16 CI jobs remain gates. Real3OS actual
parsers, qualified dependency pin, review and fresh main required before closure.
No full PM004, human or Alpha-ready claim from this bounded adapter.


## Implementation and current qualification

Actual baseline JSON value decoy produces invented function, fake table, private
route and Stripe; fail-before log preserved. Two of three extended generic
contract tests fail before adding the shared map;3/3 now pass. New-language raw
imports are neutral references, never classified third-party by syntax alone.
Full config pointer/quoted SQL names remain intact; canonical data does not reuse
raw-value code heuristics. Data-source fallback with missing Core is conservative
and empty, while existing code-language fallback remains labelled heuristic.

First actual optional smoke reaches PHP then fails because the currently installed
local K wheel predates J's parenthesized-import fix. Fixture is retained unchanged;
this is known dependency state, not a new L defect. Final actual qualification
must use K rebased onto corrected, qualified J and then K exact-main qualified.
Base/no-grammar actual smoke PASS. Current full222/222 PASS/no skips16.156s.
Quality/package/performance and exact dependency/CI/main gates still pending.

Full feature-model graph still uses its prior bounded import/route/table/technology
heuristics. This task-context unit does not qualify that other presentation or
claim that no config values can influence any product surface. Preserve remaining
canonical feature-map/route/technology adoption, richer import-member references
and source citations in the global queue. No full PM004 or Alpha closure.

Quality/package gates PASS: ExplainBench30, adversarial238/234 paths+symbols,
IdleBench5, corpus15 and installed npm package. Extended actual smoke now also
requires same-size/mtime key-edit invalidation and conservative missing-Core data
fallback. First base replay mistakenly points to J base (without K language
registrations), so admission fails on settings.json; preserve command failure.
Corrected invocation against K base passes, no fixture/production relaxation.
Optional replay still awaits corrected K dependency. Existing full222 PASS
preceded the final empty importReferences addition for unavailable files; final
full rerun is required. JSON-provider hook performance mode added to existing
gate with unchanged150/500/750/100ms budgets, execution still pending.

Actual optional-provider smoke now PASS against refreshed K wheel with J PHP
parentheses and lock fixes: all9 new families, malformed JSON, value decoys, full
key/DDL names, neutral imports, source-byte cache invalidation despite same size/
mtime, detached results and conservative missing-Core fallback. Final local
222/222 tests PASS/no skips15.674s. Dependency remains a development artifact;
publication requires qualified Core K main and fresh actual3OS CI.

Final installed package PASS. Sequential performance after K full/base suites:
JSON/Core p9588.7/max138.3/Stop124.4/state.2ms; TS/Core88.8/109.6/123.4/.2ms;
Python/Core86.8/95.8/116.8/.3ms. Actual parsed provider asserted for each; existing
150/500/750/100ms budgets unchanged. Shared host, not a controlled comparison
or monorepo/large-source guarantee. Qualified K dependency and16 CI/main gates
remain required; no final Alpha/HUMAN claim.


## PM004k dependency exact-main closure and PM004l publication

Core#109 CLOSED exact-main MACHINE: candidatef1a58d7fb622800203a76acf6a341d1bcbfab43b,
mainc8a64fb99ef051ddd35972dfe4866aa0691dee94, identical tree70da161fdec026f35b19745b5fe1e1795c45e49d.
PR test35433946187 24/24 +proof35433946174/continuity35433946193/integrated35433946120
PASS; fresh main test35434535294 24/24 +proof35434535240/continuity35434535236/
integrated35434535256 PASS, push/attempt1. Initial test35433514483 cancelled22/2
and all three initial companions PASS are retained separately. Finite-JSON review
correction and all initial failures remain in K journal; final source hashes use
PM_004K_reviewed_local.json. Reports, manifest and handoff created.

One metadata-collection invocation assumed a direct job endpoint returned content
and failed JSON parsing of undefined; rerun through supported run/jobs endpoint
succeeded. This was an agent collector error, not a product/test failure or rerun.

L workflow pins exact qualified K main. Actual installed optional/base9-family
smokes replayed against corrected K, both PASS; original PHP fixture unchanged.
Production/test source hashes remain exactly those of222/full/quality/package
local qualification. PM_004L_pinned_local.json records workflow pin and refreshed
actual smoke hashes.16 remote jobs/review/freshmain remain required for L closure.
No full feature-map, PM004, HUMAN or Alpha claim; no tag/release.


## PR review4052789918 — neutral references must reach people

Initial#12 head61471adfdd888b49e98bca0f69d8291b51623ff4/tree1daec835c9522b2912600f50dd7dae52537fb19c
passed test35435232363 16/16. Review then found importReferences absent from plain
explanations and delivery deduplication. Two explanation tests FAIL before code;
actual installed PHP smoke first fails presentation, then reordered assertions
independently reproduce a null delivery after an import-only same-size/mtime edit.
Original evidence retained; these are product defects despite initial green CI.

Existing explanation consumers now show neutral imports with origins unresolved,
without duplicating dependency names or assigning related-file imports to the
current file. Delivery keys include all bounded current and related reference
sets; unchanged observations still suppress duplicate notifications. Actual smoke
regresses PHP Vendor reference changes and a separate Ruby import-only change,
requiring both visible explanation and new delivery while dependencies stay empty.

Final27 targeted PASS; full224/224PASS16.536s, no skips. Actual installed corrected
K optional/base smokes, quality30/238/5/15 and npm package PASS. Performance after
L suites/package and O full suite, sequential on shared host, unchanged budgets:
{"budgets_ms": [150, 500, 750, 100], "json_ms": [85.9, 92.9, 117.1, 0.2], "measurement": "Sequential after L full/quality/package and Core O full suite. Shared host, no controlled comparison or worst-case guarantee.", "python_ms": [84.3, 90.1, 108.1, 0.2], "typescript_ms": [84.7, 98.1, 110.4, 0.7]}.
PM_004L_reviewed_local.json binds corrected sources/logs; original output hashes
and whitespace-normalized archived failure excerpts are distinguished. Initial
16/16 is historical; corrected16 CI/review/freshmain required. N must inherit this
presentation/dedup correction before its publication. Global NOT ALPHA READY,
HUMAN NOT RUN; no tag/release.

## Local implementation and review checkpoint

Six feature regressions fail before implementation, plus two initial strict-v2
admission failures. Shared producer admission now requests v2 explicitly and
retains the v1 default, strict source/path/hash/provider/schema/position/authority
checks and a caller budget capped at500ms. Traverse one batch per depth within
24files/640KiB/depth2, using remaining time from one extraction deadline. Retain
at most20 imports and100 symbols per file with explicit truncation counts.
These are bounded source snapshots, not an atomic whole-project/runtime proof.

Canonical Python package refs preserve raw relative targets and members; members
are not guessed to be modules. Ambiguous/outside local targets stay unresolved.
Other language origins remain unresolved until their resolver exists. Cited local
candidate edges are INFERRED; canonical symbols/imports retain syntax authority.
Configuration values cannot feed route/table/technology/import guesses even with
missing Core. Rejected canonical responses and unparsed grammar coverage stay
empty; code fallback is explicitly heuristic when Core is unavailable.

Remove unsafe whole-model cache reuse, retain public cachedFeatureModel API and
stable feature identity behavior. Each call owns a fresh bounded source model.
One old assertion demanded object identity; initial targeted run13/14PASS fails
there, and the assertion is intentionally changed to equal-but-detached values.
The new mutation/source-edit regressions explain this API behavior correction.
Local review then reproduces unchanged fingerprint after a changed canonical
symbol projection at identical source bytes: one FAIL before adding symbols to
the model fingerprint. Initial230-test/performance passes remain historical.

Final15 targeted PASS; full231/231PASS16.057s, no skips. Actual installed CoreM
wheel passes cited imports/Python ambiguity/config-value exclusion/finite-JSON/
same-size mtime-preserved edits/detached results, both optional and dependency-free.
Installed npm artifact PASS. Quality baselines ExplainBench30/adversarial238
(234 exact paths and symbols)/IdleBench5/corpus15 PASS before fingerprint-only
review correction; remote quality must replay final source. Final sequential
provider performance after suites/package: Pythonp9585.4/max93.0/Stop189.4/state.2ms,
TypeScript86.4/89.6/205.5/.2ms, JSON88.3/91.5/211.9/.5ms. Unchanged150/500/750/100ms
budgets; shared host, no controlled comparison or worst-case/monorepo guarantee.

Workflow now contains actual feature smokes with and without grammars, but its
Core pin is intentionally not yet the final dependency: publication waits for
qualified L and M mains. Inherited older pin must be replaced before CI. Local
M wheel inherits K finite-JSON correction, detailed contract profile8. No remote
N qualification, full PM004/HUMAN/Alpha claim. Missing CommonJS/dynamic imports,
canonical code surfaces and final source-preserving presentation remain follow-up.
