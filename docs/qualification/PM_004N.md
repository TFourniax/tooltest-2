# PM-004n — canonical feature imports, data facts and fresh model reads

Acceptance before implementation,2026-09-19. Owned development parent L ef797f9;
publication waits for qualified L and Core M mains. No remote qualification yet.
Global NOT ALPHA READY; MACHINE only; HUMAN NOT RUN.

Actual discovery on the existing feature model: a commented JS import creates an
edge, JSON values create route/table/Stripe surfaces, and cachedFeatureModel at
an unchanged event returns the same stale mutable object after source edits.
Original inline discovery harness has a syntax typo before executing; corrected
script records actual fixture output, not a passing or failing product test.

Reuse buildFeatureModel, shared source admission and Core byte transport. Add
strict opt-in v2 admission to the existing consumer without changing default v1
calls. Canonical imports and source citations drive local candidate edges; one
batch per traversal level, at most existing depth2/24files/640KiB, with a single
500ms extraction deadline across levels. No project execution, network resolver,
real Git-index writes or widened source bounds. Ambiguous/outside candidates stay
unresolved; local candidate edges are INFERRED, never runtime/Proof authority.
Member names are syntax references, not declarations that they are modules.

For SQL/config use only admitted DDL/key facts. Literal values cannot generate
route/table/technology or import facts even if Core is missing. Canonical invalid
or missing grammars remain explicit empty coverage. Code-language API/SQL-string/
technology surface heuristics remain a separate follow-up: preserve compatibility
but label them INFERRED, with source hash and explicit extraction coverage, never
present them as proven external boundaries. This does not close their canonical
migration or full PM004. Existing schema/story consumers retain usable adapters.

Remove reuse of an unchecked mutable feature result. Preserve the public cached
function API while rebuilding from bounded fresh reads and returning independent
objects; optimize again only with a measured, content-bound cache. Retain durable
feature history as historical observations; do not infer human understanding.
Model fingerprints include inspected source hashes and extraction coverage.

Regress comment/string import decoys, JSON values, source/member/position/authority
v2 admission, package-aware relative references, ambiguous and outside paths,
same-size/mtime edits, object mutation, absent/malformed Core, optional grammar
fallback, source exclusion and traversal bounds. Existing full quality/package
and unchanged150/500/750/100ms hook budgets,16 CI/review/freshmain required. Real
installed M producer must be exercised, then final qualified dependency pin.

CJS/dynamic import references, complete syntax-derived routes/data/technology,
resolved package origins, final source-preserving Portal presentation, all larger
longitudinal PM and Alpha/human/distribution gates remain explicit follow-ups.


## Local implementation and review checkpoint

Six feature regressions fail before implementation, plus two initial strict-v2
admission failures. Shared producer admission now requests v2 explicitly and
retains the v1 default, strict source/path/hash/provider/schema/position/authority
checks and a caller budget capped at500ms. Traverse one batch per depth within
24files/640KiB/depth2, using remaining time from one extraction deadline. Retain
at most20 imports and100 symbols per file with explicit truncation counts.
These are bounded source snapshots, not an atomic whole-project/runtime proof.

Canonical Python package refs preserve raw relative targets and members; members
are not guessed to be modules. Ambiguous/outside local targets stay unresolved.
Other language origins remain unresolved until their resolver exists. Cited local
candidate edges are INFERRED; canonical symbols/imports retain syntax authority.
Configuration values cannot feed route/table/technology/import guesses even with
missing Core. Rejected canonical responses and unparsed grammar coverage stay
empty; code fallback is explicitly heuristic when Core is unavailable.

Remove unsafe whole-model cache reuse, retain public cachedFeatureModel API and
stable feature identity behavior. Each call owns a fresh bounded source model.
One old assertion demanded object identity; initial targeted run13/14PASS fails
there, and the assertion is intentionally changed to equal-but-detached values.
The new mutation/source-edit regressions explain this API behavior correction.
Local review then reproduces unchanged fingerprint after a changed canonical
symbol projection at identical source bytes: one FAIL before adding symbols to
the model fingerprint. Initial230-test/performance passes remain historical.

Final15 targeted PASS; full231/231PASS16.057s, no skips. Actual installed CoreM
wheel passes cited imports/Python ambiguity/config-value exclusion/finite-JSON/
same-size mtime-preserved edits/detached results, both optional and dependency-free.
Installed npm artifact PASS. Quality baselines ExplainBench30/adversarial238
(234 exact paths and symbols)/IdleBench5/corpus15 PASS before fingerprint-only
review correction; remote quality must replay final source. Final sequential
provider performance after suites/package: Pythonp9585.4/max93.0/Stop189.4/state.2ms,
TypeScript86.4/89.6/205.5/.2ms, JSON88.3/91.5/211.9/.5ms. Unchanged150/500/750/100ms
budgets; shared host, no controlled comparison or worst-case/monorepo guarantee.

Workflow now contains actual feature smokes with and without grammars, but its
Core pin is intentionally not yet the final dependency: publication waits for
qualified L and M mains. Inherited older pin must be replaced before CI. Local
M wheel inherits K finite-JSON correction, detailed contract profile8. No remote
N qualification, full PM004/HUMAN/Alpha claim. Missing CommonJS/dynamic imports,
canonical code surfaces and final source-preserving presentation remain follow-up.


## Reviewed N local checkpoint, 2026-09-20

Canonical relative references reuse Core's normalized targets; leading dots
remain unresolved, with exact raw citations retained. JS candidate ambiguity
now includes CTS/MTS without guessing Python extensions. Both defects reproduced
before correction, including actual installed producer for Python. Rebased N-only
commits onto qualified L main6109ffec with identical tree before/after rebase.

Final13 targeted and235/235fullPASS24.988s, optional/base actual consumer, quality
30/238(234exact paths/symbols)/5/15 and installed npm artifact PASS. Earlier
231/233/234 results remain historical. PM_004N_reviewed_local.json binds sources.
Sequential Python performance FAIL155.5ms against unchanged150ms. Qualified L
baseline using same installed M/O wheel also FAIL220.5ms; no cause concluded.
TypeScript149.4/max180.9/Stop324.6/state.2 and JSON146.8/320.1/328.1/.3ms PASS.
All measurements shared-host, no controlled comparison/worst-case guarantee.
Do not transfer prior passing Python metrics. Exact CI performance remains a gate.

Core M/O PR110 candidate e56a5a7884072ea51f29d9475235fd942f70c992 is under24+3
qualification. N publication still waits for qualified producer main and the
exact workflow pin; inherited c8a64fb pin cannot run the richer fixtures. No
remote N/HUMAN/Alpha qualification, no tag. Continue full#74 after closure.


## Publication candidate, resumed 2026-09-20

Qualified producer is now Core main 8c4f879688efbd3019c92a25349ff1426a8cc4f1,
tree a38864d35e01a0687f0ccbbf2d75a0e055c4a77f (PR110; fresh push24+3PASS).
All96 installed Python source files match that main byte-for-byte. The workflow
pin now selects it; the installed feature smoke includes escaped direct eval.
IdleProof base remains qualified L main6109ffec2b98fd2c76962382d5f63c2a7a7576e8.

Fresh235/235 tests PASS28.558s, no skips; actual optional consumer, Explain30,
adversarial238/234exact, IdleBench5/corpus15 and installed npm artifact PASS.
No product source changed since the reviewed448ddec checkpoint; final changes
are exact dependency pin and the additional real escaped-eval scenario. Prior
Python155.5ms and L220.5ms FAIL measurements remain historical evidence; a fresh
sequential final budget check and exact16 CI/review/freshmain are still required.
No HUMAN PASS, complete PM004, global readiness or release claim.


## PR13 review4057109049 — mixed unsupported source batches

Initial PR17b7f394dd/tree62432a3 passes16 CI jobs35514848184; preserve as
historical, not qualification of this correction. Reviewer reproduced missing
Vue fallback when an unrelated supported file joins a traversal level. New
regression FAIL before correction; eligibility and coverage reason are now
per-file. Unsupported files retain explicitly INFERRED legacy heuristics;
rejected/unparsed supported files still cannot resurrect legacy guesses.
14 targeted and236/236fullPASS22.635s, real optional/base mixed-file journey,
quality30/238/5/15 and package PASS. Local actual Python againFAIL158.5/150ms;
original CI actualPython122.0/TS127.9/JSON127.0msPASS. These different-host
observations are retained without claiming N caused a performance regression.
Separate Core startup discoveryPM012d measures unrelated eager imports and
will be qualified independently. N's current Core pin and budgets stay fixed.
Fresh corrected16/review/freshmain required; no HUMAN/global readiness claim.
