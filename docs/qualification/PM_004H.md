# PM-004h — shared syntax in IdleProof task context

Acceptance before implementation, 2026-09-19. Parent IdleProof main30a2c53;
required Core dependency is PM-004g qualified exact main, not an unqualified
development ref. Global NOT ALPHA READY; MACHINE only; HUMAN NOT RUN.

Extend the existing bounded source-byte consumer and task context to the canonical
JS/TS/Go/Rust providers. Reuse admitted source reading, one bounded argv subprocess,
strict response validation and detached byte-keyed cache. Preserve Python exports
and behavior. Bind expected language/provider/module to each admitted path, retain
source hashes, positions and epistemic authority, and reject a malformed mixed
batch completely. No shell, new transport/store, project execution or runtime
dependency installation.

Canonical unparsed files remain empty with explicit coverage. Missing/older Core
retains existing explicitly labelled heuristic fallback; unsupported languages
remain adapter-pending. Display actual terminal symbol names rather than path
fragments, and retain non-Python textual dependency targets. Routes/tables/technology
and feature-model graph adoption remain separate open work; do not claim full
consumer unification or semantic coverage from this bounded change.

Reproduce false string/template/raw-string declarations before implementation.
Test mixed-language response binding, source/order/provider/module/authority
tampering, malformed syntax, missing grammars, same-size/time edits and detached
cache values. Actual installed Core tests on three OS must include all four
optional languages; skipped or mocked grammar results do not qualify behavior.
Existing16 CI jobs, full quality/package/performance budgets, review and fresh
exact-main replay remain required. Do not raise500ms subprocess or hook budgets.
Core exact-main qualification is required before publication/merge. No tag/release.

## Local implementation and qualification

One shared validator/loader now selects the expected provider from each admitted
extension, validates its exact module/path and non-Python symbol namespace, and
checks the existing source/line/authority/coverage contract. Python-only exports
remain compatibility wrappers around the same admission. Suffix matching follows
Core exactly, including case and dotfile handling. No optional dependency is
installed by IdleProof at runtime.

Context displays terminal symbol names without path fragments and retains native
and JS/TS textual imports. Cache values remain detached and include source bytes
and admitted provider result/coverage. The existing source protections, configured
read-only command lookup and assurance-required behavior remain intact.

Before implementation, actual Core plus the old consumer selects invented from
a TypeScript template instead of actual. Three new contract tests then fail
because the generic adapter is absent. All nine consumer contract tests pass
afterward. The first actual smoke harness asserted TypeScript edits while the
existing selection rule correctly chose the last touched Rust file; declare the
read capability to select the current TypeScript file. The initial harness
failure is retained and is not reported as a product defect.

Local full222/222 PASS, no skips(15.086s). Actual mixed-language and missing-grammar
consumer journeys pass, including string/raw-string decoys, malformed syntax,
same-size/time edits, detached caches and no source persistence. Explain30,
adversarial238, IdleBench5, corpus15 and npm package contents PASS. Shared-host
diagnostics: ordinary hook p952.7/max7.3/Stop36.1/state.4ms;
TypeScript/Core89.3/92.6/116.3/.2ms; Python/Core92.3/99.8/115.4/.5ms.
Budgets150/500/750/100ms and subprocess500ms remain unchanged. No universal or
all-input latency guarantee; these are small real fixtures.

Existing16 CI jobs retained. ThreeOS real-Core consumers now verify dependency-
free unparsed coverage, install the exact optional grammar pins and verify actual
mixed-language extraction. Ubuntu qualifies Python and TypeScript budgets. The
Core ref must be changed to PM004g's qualified main before this work is published.
Exact-source review, PR/full-main qualification and unit closure remain pending.

Core dependency updated to merged#106 main0c342bc2fb12edb2d58da39958e3c0667a22a1db,
tree07866b4c51b1670aa26b0267b48eefa4bc45f9c7; fresh-main gate still required
before publication. Rechecked actual consumer and budgets against corrected
source: TypeScript p9588.2/max99.4/Stop118.1/state.2ms; Python
83.6/87.8/118.7/.2ms, all PASS. Prior samples retained, no budget changes.

## Review correction — local/platform imports

Initial PR#11 head d1062268a35e01f34130ccbcca2d021a58432c67 had CI35412716165
16/16 PASS. Review4051821830 nevertheless reproduced local and platform imports
becoming third-party integration signals. Initial green qualification is retained
and is not transferred to the correction. Actual Core reproduction and new smoke
both FAIL before the fix; logs are archived.

Canonical extraction preserves every raw import. At the task dependency boundary,
filter relative/absolute paths, package-internal # names, Node/file/data prefixes,
Node built-ins, Rust crate/self/super/std/core/alloc, and undotted Go module roots.
Node isBuiltin is a runtime metadata query, not project/module execution:
https://nodejs.org/api/module.html#moduleisbuiltinmodulename
Actual TS/Rust/Go checks require empty dependencies/core role for local/platform
only sources, retain candidate external references, and verify raw imports remain.
The regression now runs with real Core on all three OS in the existing16 jobs.

This is conservative textual selection, not semantic package-origin resolution.
JS aliases, host-qualified Go self-modules and Python package origin remain open;
raw observations do not establish third-party ownership, package execution or Proof.
No resolver, shell, runtime dependency install or second extraction system added.

Corrected local222/222 PASS, no skips,15.844s; quality30/238/5/15 and installed
npm package PASS. Actual optional/base consumers PASS. Fresh sequential shared-host
diagnostics after the suites: TS p9585.4/max92.1/Stop132.7/state.4ms;
Python79.9/88.0/117.1/.2ms. Existing budgets and500ms subprocess unchanged.
Corrected-head CI, review and fresh exact-main qualification remain pending.
