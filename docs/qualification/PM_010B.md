# PM-010b — exact Portal memory citations

Baseline IdleProof 2b919f7dddeaf3488091017a46f079d8c9718056. Core assertions
already carry exact project-event IDs and hashes; the Portal projection dropped
them and truncated all identities to 96 characters. Two local regression tests
fail on the baseline: distinct long IDs collapse, and a secret-bearing ID becomes
a redacted alias. Source loss was confirmed by inspecting the actual projection.

Preserve the source allowlist and complete admitted identities. Omit any entity
or relation whose identity would need privacy redaction; report the omission in
a bounded warning. Do not alter labels' redaction, statuses, source hashes,
confirmation identities, journal bytes, or the 64 KiB snapshot budget. Legacy
contexts remain uncited, without manufactured sources.

Paired Portal consumer admits the additive source on all five assertion kinds,
checks confirmation/assertion consistency, and displays complete IDs/hashes in
guided and technical modes. Qualify and deploy that consumer before distributing
this producer; older strict consumers reject new source fields. Never strip
citations from queued data or change its content-addressed identity for fallback.

Local targeted suite 10/10 and full suite 253/253 pass. Durable raw logs are in
evidence/PM_010B_idle-citations-*.log. Native public CLI smoke now checks exact
source transport. PR review, cross-platform matrix and fresh main gates remain
required. Full PM-010/013 and HUMAN are still open.

## Review corrections

Review4080844425 reproduced an underscore-delimited assignment/token bypass
past the old96-character cutoff. Identity scanning now ignores display-text
word boundaries and omits the entire affected entity/relation. Five credential
shapes are rejected at the producer, including underscore-delimited assignment,
OpenAI/GitHub-style tokens and AWS access-key-like values. The paired Portal
consumer applies the same stricter identity admission. Original failure retained.

Review4080844435 reproduced a dense valid context exceeding64KiB after exact
citations were retained. Budget the complete snapshot including its final ID
width. Deterministically omit whole lower-priority rows, retaining ranked task
and assertion rows longest, and add an explicit reduction warning. IDs, hashes,
labels and review references of retained rows remain byte-for-byte equal to the
privacy-safe projection. No journal/local context or other snapshot field is
changed. The wire limit is unchanged; oversized non-memory content still fails
closed. The regression includes multibyte labels, full admitted row counts/long
IDs and forty long file paths; its original overflow failure is retained.

Updated targeted suite12/12 and full255/255 pass locally. Earlier PR run
35841075772 passed16/16 on b543b48, but is historical evidence for this follow-up.
Fresh review/CI/main are required. Portal#34 main0e12c644f1f65f399af76a70ef81a684b5fc64cd
has fresh35841643131 SUCCESS7/7; no deployment or HUMAN.

Review4080922901 found the product's own ipd_ device credential family absent
from the inherited credential patterns. Source validation in portal-client.mjs
and both Portal auth endpoints confirms this format. The exact-ID regression
failed before adding the native family; the scanner now rejects the entire
identity and redacts this family in display text/warnings too. Case variants
accepted by the server and the20-character minimum are covered. No real token
is used by the fixtures.12targeted/255full tests pass; failure/after/full logs
are retained. Prior a7b9b89 run35842583881 passed16/16 but does not qualify this
new correction. Fresh review and CI remain required.
