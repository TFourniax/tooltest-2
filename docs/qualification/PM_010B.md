# PM-010b — exact Portal memory citations

Baseline IdleProof 2b919f7dddeaf3488091017a46f079d8c9718056. Core assertions
already carry exact project-event IDs and hashes; the Portal projection dropped
them and truncated all identities to 96 characters. Two local regression tests
fail on the baseline: distinct long IDs collapse, and a secret-bearing ID becomes
a redacted alias. Source loss was confirmed by inspecting the actual projection.

Preserve the source allowlist and complete admitted identities. Omit any entity
or relation whose identity would need privacy redaction; report the omission in
a bounded warning. Do not alter labels' redaction, statuses, source hashes,
confirmation identities, journal bytes, or the 64 KiB snapshot budget. Legacy
contexts remain uncited, without manufactured sources.

Paired Portal consumer admits the additive source on all five assertion kinds,
checks confirmation/assertion consistency, and displays complete IDs/hashes in
guided and technical modes. Qualify and deploy that consumer before distributing
this producer; older strict consumers reject new source fields. Never strip
citations from queued data or change its content-addressed identity for fallback.

Local targeted suite 10/10 and full suite 253/253 pass. Durable raw logs are in
evidence/PM_010B_idle-citations-*.log. Native public CLI smoke now checks exact
source transport. PR review, cross-platform matrix and fresh main gates remain
required. Full PM-010/013 and HUMAN are still open.
