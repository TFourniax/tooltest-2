# Work journal — 2026-09-18

- Resumed coordinated Alpha work under the user's standing instruction to keep
  repository traceability and merge qualified PRs, then verify fresh exact main.
- Inspected main 0a6d6eaa7bf5afab72e185be0d0d97d3b679cd5f. Identified shallow
  context admission, omitted Core tasks/lifecycle/warnings, and dropped Core
  string relation endpoints in the Portal projection.
- Opened bounded PM-010a consumer work; acceptance recorded before implementation
  in PM_010A_CONTEXT_CONSUMER.md. No Alpha or HUMAN PASS claim.
- Baseline new contract tests: 4/5 FAIL; unavailable Core remains graceful (1 PASS).
  Initial typed admission, task query and rendering/projection changes: 10/10
  focused + existing task tests PASS. Added coercion/oversize/authority/query
  mismatches and visible truncation coverage during review.
- Real public Core CLI interoperability PASS locally: declared task retrieval,
  confirmed decision with separate DECLARED review, bounded Portal projection,
  and retirement exclusion. The first harness invocation incorrectly used
  `decision add`; corrected to the existing `decision record` interface.
  Added the real CLI journey to all three CI operating systems, pinned to exact
  Core main 897bd146b07fcbe1a26e00d0b1306630d1032483. Dependency qualification
  remains a merge prerequisite.
- Full local Node suite initially 182/183 PASS, demo readiness exceeds 15s.
  An added truncation assertion initially used a budget larger than its fixture;
  corrected to exercise real truncation. Exact baseline main also reproduces demo
  timeout (177/178 PASS, 32.098s); candidate demo alone PASS (10.361s). Test-file
  resource contention is reproducible independent of these source changes.
  Trial with four concurrent test files pending, no timeout increase or test skip.
- Four concurrent files also reproduce the same demo timeout (182/183 PASS,
  44.115s). Serialize test files in the canonical npm test runner: every assertion
  and the 15s readiness bound remain unchanged, while independent realistic Git/
  HTTP journeys no longer saturate the same Windows host. Full serial run pending.
- Canonical serial suite now PASS: 183/183, no skip, 107.285s. The complete
  contract/privacy/task/Portal/HTTP/native journey coverage runs unchanged.
- Core PM-006 dependency is now exact-main qualified: test 35329400911 attempt 2
  PASS 21/21; ProofBench 35329400851, ContinuityBench 35329400870 and integrated
  35329400874 PASS. Attempt 1 Windows hook timeouts remain preserved in Core's
  evidence archive. No HUMAN PASS is inferred from a retry or scripted journey.
- ExplainBench PASS (30 scenarios); adversarial Explain TortureBench PASS (238
  project forms); IdleBench PASS (5 quality cases); corpus PASS (15 domains).
- Local Windows performance gate FAIL: Stop/handoff 1414.2ms vs 750ms. Sequential
  exact-baseline measurement also FAILS at 1329.4ms. Neither sample establishes a
  consumer regression or speedup. This pre-existing Windows performance gap is
  kept open for PM-012 follow-up; the canonical remote quality gate uses Ubuntu.
- PM-010a installed package smoke PASS on Windows/Node 24.19.0. PR #5 opened at
  e2329f0b1bc259dad2f34de3375e2c0ee0fdf9e0; 16-job remote qualification pending.
- Started separate PM-012i snapshot efficiency branch, acceptance recorded before
  changes. Profiling isolates 20 Git processes / 1213.6ms of 1277.4ms Stop total.
  Check HEAD/tree races and exclusion failure along with reducing process count;
  retain real-index isolation, literal paths and existing identity algorithms.
- Four PM-012i regressions all FAIL before correction (4.896s). In addition to
  excessive process count, reproduced wildcard interpretation of transient names,
  silently accepted exclusion failure, and a real HEAD advance attaching an old
  commit SHA to a new worktree tree. These are identity/source-binding defects;
  no forged executed DiffWitness Proof is claimed.
- Correction batches literal NUL-delimited exclusions and pins tree resolution to
  the captured commit, uses one status record for HEAD/paths and combines diff
  with stats; local identity config reads share one process. No persistent cache
  or truth-boundary bypass. Validation in progress.
- Initial corrected Windows Stop still FAILS at 812.2ms vs 750ms. Revised path
  admission filters transient untracked paths before adding to the disposable
  HEAD index, avoiding a second pass/reset; tracked files and deletions remain
  meaningful, including explicitly committed runtime-named files. Staging a new
  transient file no longer circumvents its exclusion. Real index remains intact.
- Unicode rename regression separately reproduced an invented quoted path alias;
  NUL-delimited status/stat parsing now retains actual paths and exact patch bytes.
  Expanded identity/analysis/ownership set 21/21 PASS (15.715s). Unchanged Windows
  performance gate now PASS: hook p95 17.0ms, max 21.5ms, Stop 684.8ms, state p95
  0.7ms. Full suite and remote qualification pending; no universal latency claim.
- PM-010a PR #5 merged at 2026-09-18T10:04:35Z to
  3fb315d153f6de6e89212f144e09df7a286c78a8, identical tree
  8653cc80e64d70ec17e237f795b57c414c898ff7. Exact PR test 35332100069 and fresh-main
  test 35332836518 both PASS 16/16. Bounded consumer MACHINE unit qualified;
  Windows snapshot/latency correction is this separate PM-012i unit. HUMAN NOT RUN.
- PM-012i #6 exact-head test 35333831475 and fresh-main 35334270243 PASS 16/16.
  Main e008a720bcfc63b0eb37e513e43cdf7e60d7ce87, tree
  6e390f67776fdd338d564d08c6438750ae7ef787. HUMAN NOT RUN.
- PM-002c acceptance recorded before implementation: align native task identity
  with Core's existing 12,000-code-point prefix and scalar-safe temporary anchors;
  preserve saved task identity/history and separate legacy receipt prompt fields.
- Three PM-002c Unicode/long-prompt regressions fail before correction. Corrected
  native task prefix, scalar count/compaction and whitespace now pass 8/8 targeted
  tests (123ms). Existing task anchors/IDs remain unchanged on followups. Expanded
  real Core smoke checks 12 native prompts, followups and pivots with durable CLI
  ID/digest/count comparisons; Core fixture repinned to qualified de248b10.
- Full sidecar suite before native failure correction PASS 192/192 (106.370s).
  Actual Unicode interoperability initially fails on Windows Core cp1252 input;
  Core #98 corrects it. With that candidate, all 12 actual native prompts PASS.
  Final CI fixture must include Core #98 before this coordinated unit qualifies.
- PM-P0-002 two regressions FAIL before correction: split UTF-8 chunks corrupt
  an accented character; unrecoverable sidecar state makes Claude Stop exit 0
  with no decision before required Core configuration is evaluated. Acceptance
  recorded in PM_P0_NATIVE_FAILURE.md. Shared input reader was first extracted
  without behavioral changes to reproduce the existing chunk defect.
- Incremental strict UTF-8 decoding now preserves valid chunk splits. Advisory
  lifecycle failures are contained/reported, required Core checks still run, and
  optional queue failures cannot erase an already computed native result.
- Full revised sidecar suite PASS 195/195 (95.956s), zero skips. Expanded native
  failure regression passes seven real runner invocations: Claude/Codex invalid
  config, missing engine, throwing invalid command, then standalone availability.
  Every multibyte split boundary passes; invalid UTF-8/BOM are rejected. Final
  focused 3/3 PASS (1.080s). Core interop fixture pins #98 e882f7c candidate;
  its remote and fresh-main qualification are dependencies before merging here.
- PM-004 local source boundary acceptance recorded before implementation in
  PM_004_SOURCE_BOUNDARIES.md. Four regressions FAIL before correction: external
  directory junction leaks a synthetic route; an alias exposes excluded local
  state; invalid UTF-8 is admitted; growth after stat bypasses the byte limit.
  Two compatibility/boundary cases pass (6 tests, 231.844ms). Fixtures are owned
  temporary directories only. Canonical path admission and bounded descriptor
  reads implemented; qualification in progress, HUMAN NOT RUN.
- Expanded source-boundary suite PASS 9/9 (290.826ms), including short descriptor
  reads, growth during reading, closed descriptor, alias retargeting and total
  byte accounting. Full suite PASS 204/204 (95.160s), zero skips.
- Core #98 fresh main 672fe947f53d2c0d9bf5e0b8280bc8c3292b0aa5 qualifies:
  test 35339123260 PASS 21/21; ProofBench 35339123349, ContinuityBench 35339123377
  and integrated 35339123329 PASS. IdleProof #7 was then merged at
  2026-09-18T11:34:10Z to d2fb2ce46d129c879f20dc838b4c0e5a3834fc1a, identical
  tree d4c8acdb74baaa580305d44a1a2055d0f80ad226. Exact PR test 35337365070
  and fresh-main test 35340278031 both PASS 16/16. Manifests archived here.
  Unicode identity/required-assurance unit MACHINE qualified; HUMAN NOT RUN.
- PM-004 source PR #8 first exact head d8f949f7c4bd74e24152c27d4517b81688938dcd
  FAILS remote test 35340567805: 13/16 jobs pass, all three macOS unit jobs fail
  two race-injection cases. The fixture compares a temporary /var alias with
  the product's /private/var canonical target, so no mutation is injected.
  Canonicalize the fixture root before setting up those assertions. Product
  implementation and rejection assertions are unchanged; failure manifest is
  preserved. No merge until the corrected exact head is fully qualified.
- Corrected source-fixture tests PASS 9/9 (297.794ms). New exact-head test
  35341450926 passes all macOS jobs but FAILS 15/16 overall: Windows/Node 24
  concurrent session journey raises EPERM in state.lock mkdir. This exposes a
  separate real product race: the existing contention classifier rejects a
  transient error if the previous lock was removed before its existence probe.
  PM_011_LOCAL_LOCK.md records acceptance before correction. Two targeted tests
  FAIL before the fix; the non-contention failure case passes (128.843ms).
  Retry transient acquisition errors under the unchanged 15s deadline, preserving
  failure for permanent denial and mutation only after actual lock ownership.
- Corrected lock/contention/recovery set PASS 7/7 (15.921s), including the real
  24-child/8-session journey and complete provenance-chain count. Full sidecar
  suite PASS 207/207 (104.403s), zero skips. PR #8 now includes this separately
  documented local lock fix; earlier two remote failures remain archived.
